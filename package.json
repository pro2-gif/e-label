{
  "name": "e-label",
  "version": "1.0.0",
  "description": "다국어 e-라벨 화면",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview",
    "cf:deploy": "vite build && wrangler pages deploy dist --project-name e-label --branch main",
    "cf:preview": "vite build && wrangler pages dev dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "vite": "^5.0.0",
    "wrangler": "^4.125.0"
  },
  "dependencies": {
    "jsdom": "^30.0.1"
  }
}
